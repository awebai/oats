Hand-built, PROVISIONAL. The rows follow the kernel's row builders at 27413bcb
(#213): `lib/triggers.mjs` `triggerRow`/`listTriggers` and `lib/schedule.mjs`
`scheduleRow`/`listSchedules`, plus the contract in docs/desktop-cli-api.md
"Workspace triggers and schedules". Replace them with captures from a real
`oats trigger list --json` / `oats schedule list --json` once the Desktop
server serves them (oats-desktop-engineer-1).
