# Instance briefing: dev-claude-checkout-kstub-mstub

You are instance "dev-claude-checkout-kstub-mstub" of agent "dev".
- Home: <base>/scope/agents/dev/instances/dev-claude-checkout-kstub-mstub
- Team: golden-team (golden-team-id) — see teammates with `oats status --team`
- Work tree: ./work — a symlink to the <base>/scope/repo checkout — you share it; work on the currently checked-out branch (main) and do not switch branches without being asked
- Do all repository work inside ./work. Read ./work/AGENTS.md or ./work/CLAUDE.md first if present.
- Knowledge: keep STATE.md current and drop working notes in notes/.
- Messaging: you are reachable as dev-claude-checkout-kstub-mstub.

## Task

Freeze the externally meaningful outputs of spawn and retire.
