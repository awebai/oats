# Instance briefing: dev-pi-worktree-kstub-mnone

You are instance "dev-pi-worktree-kstub-mnone" of agent "dev".
- Home: <base>/scope/agents/dev/instances/dev-pi-worktree-kstub-mnone
- Team: golden-team (golden-team-id) — see teammates with `oats status --team`
- Work tree: ./work — a dedicated git worktree of <base>/scope/repo on branch "agents/dev-pi-worktree-kstub-mnone" — commit freely there
- Do all repository work inside ./work. Read ./work/AGENTS.md or ./work/CLAUDE.md first if present.
- Knowledge: keep STATE.md current and drop working notes in notes/.

## Task

Freeze the externally meaningful outputs of spawn and retire.
