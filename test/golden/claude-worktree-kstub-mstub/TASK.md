# Instance briefing: dev-claude-worktree-kstub-mstub

You are instance "dev-claude-worktree-kstub-mstub" of agent "dev".
- Home: <base>/scope/agents/dev/instances/dev-claude-worktree-kstub-mstub
- Team: golden-team (golden-team-id) — see teammates with `oats status --team`
- Work tree: ./work — a dedicated git worktree of <base>/scope/repo on branch "agents/dev-claude-worktree-kstub-mstub" — commit freely there
- Do all repository work inside ./work. Read ./work/AGENTS.md or ./work/CLAUDE.md first if present.
- Knowledge: keep STATE.md current and drop working notes in notes/.
- Messaging: you are reachable as dev-claude-worktree-kstub-mstub.

## Task

Freeze the externally meaningful outputs of spawn and retire.
